<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <%= if Helpers.is_logged_in?(session) %>
    <p>Welcome <%= Helpers.current_user(session).name %></p>
    <p> Your balance: <%= Helpers.current_user(session).balance %></p>
    <%=
      <h1>Log Out <a href="/logout">Log Out</a></h1>

  </body>
</html>
